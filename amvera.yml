meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  artifacts:
    '*': dist
  command: node dist/main
run:
  scriptName: dist/main.js
  persistenceMount: /data
  containerPort: 3000
